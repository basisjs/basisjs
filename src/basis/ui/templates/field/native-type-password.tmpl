<b:style src="./native-type-password.css"/>
<b:define name="validity" type="enum" values="invalid"/>

<input{field|focus}
  type="password"
  class="native-type-password native-type-password_{validity}"

  name="{name}"
  value="{value}"
  readonly="{readonly}"
  disabled="{disabled}"
  maxlength="{maxlength}"
  placeholder="{placeholder}"

  event-keydown="keydown"
  event-keyup="keyup"
  event-keypress="keypress"
  event-focus="focus"
  event-blur="blur"
  event-change="change"
  event-input="input"
/>
