<input{field|focus}
  type="text"
  class="native-type-text"

  name="{name}"
  value="{value}"
  readonly="{readonly}"
  disabled="{disabled}"
  maxlength="{maxlength}"
  autocomplete="{autocomplete}"
  placeholder="{placeholder}"

  event-keydown="keydown"
  event-keyup="keyup"
  event-keypress="keypress"
  event-focus="focus"
  event-blur="blur"
  event-change="change"
  event-input="input"
/>
